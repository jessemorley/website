# Redirect clean URLs to index.html with hash fragments
/info    /index.html#info    302
/contact /index.html#contact 302

# Note: .html files are NOT redirected here so JavaScript fetch() can access them
# The JS redirect in each .html file handles direct browser access